# Instagram Comment API

REST API на Django для публикации комментариев к постам через Instagram Graph API.

## 🚀 Быстрый старт

### Требования
- [Docker Desktop](https://www.docker.com/products/docker-desktop/)

### Запуск проекта
```bash
docker-compose up --build
```

Приложение будет доступно по адресу: `http://localhost:8000`

---

## 📁 Структура проекта

```
instagram_project/
├── Dockerfile
├── docker-compose.yml
├── requirements.txt
├── manage.py
├── pytest.ini
├── config/
│   ├── settings.py          # Настройки Django
│   └── urls.py              # Корневые URL
└── app/
    ├── models/              # Модели Post, Comment
    ├── services/
    │   ├── instagram_client.py  # HTTP-клиент Instagram API
    │   └── comment_service.py   # Бизнес-логика
    ├── serializers/         # DRF сериализаторы
    ├── views/               # API Views
    ├── migrations/          # Миграции БД
    └── tests/
        └── test_comment_create.py  # Интеграционные тесты
```

---

## 📌 API Endpoints

### Создать комментарий

```
POST /api/posts/{id}/comments/
```

**Тело запроса:**
```json
{
  "text": "Текст комментария"
}
```

**Успешный ответ (201):**
```json
{
  "id": 1,
  "post": 1,
  "instagram_comment_id": "17858893269000001",
  "text": "Текст комментария",
  "created_at": "2026-02-18T12:00:00Z"
}
```

**Ошибки:**

| Код | Причина |
|-----|---------|
| `400` | Неверное тело запроса |
| `404` | Пост не найден в БД или удалён в Instagram |
| `502` | Ошибка Instagram API |

---

## 🏗 Архитектура

Проект построен по принципу разделения ответственности:

- **Views** — только HTTP (парсинг запроса, формирование ответа)
- **CommentService** — бизнес-логика (проверка поста, сохранение в БД)
- **InstagramClient** — изолированный HTTP-клиент для Instagram Graph API

---

## 🧪 Тесты

```bash
docker-compose exec web pytest app/tests/ -v
```

### Покрытие тестами (12 тестов)

| Сценарий | Тесты |
|----------|-------|
| ✅ Успешное создание комментария | 3 теста |
| ✅ Пост не найден в БД | 3 теста |
| ✅ Медиа удалено в Instagram | 3 теста |
| ✅ Валидация входных данных | 3 теста |

> Реальные HTTP-запросы к Instagram API не выполняются — используется `unittest.mock`

---

## ⚙️ Переменные окружения

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `SECRET_KEY` | Django secret key | `django-insecure-dev-key` |
| `DEBUG` | Режим отладки | `True` |
| `INSTAGRAM_ACCESS_TOKEN` | Токен Instagram Graph API | — |
| `DATABASE_URL` | URL базы данных | PostgreSQL в Docker |

---

## 🛠 Технологии

- **Python 3.11**
- **Django 4.2**
- **Django REST Framework 3.14**
- **PostgreSQL 15**
- **Docker / Docker Compose**
- **pytest + pytest-django**
